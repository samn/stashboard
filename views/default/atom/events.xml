<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:sb="http://www.stashboard.org">
    <title type="text">{{ config.SITE.title }}</title>
    {% if service %}
        <subtitle type="html">{{ config.SITE.description }} - latest {{ events_count }} events for service {{ service.name }}</subtitle>
    {% else %}
        <subtitle type="html">{{ config.SITE.description }} - latest {{ events_count }} events</subtitle>
    {% endif %}
    <id>tag:{{ host }},2010:/{{ path }}</id>
    <updated>{{ updated|date:"Y-m-d\TH:i:s\Z" }}</updated>
    <link rel="alternate" type="text/html" hreflang="en" href="{{ config.SITE.root_url }}/" />
    <link rel="self" type="application/atom+xml" href="{{ url }}" />
    {% for event in events %}
    <entry>
        <id>tag:{{ host }},2010:{{ event.key }}</id>
        <title>{{ event.service.name }} - {{ event.status.name }}</title>
        <updated>{{ event.start|date:"Y-m-d\TH:i:s\Z" }}</updated>
        <content>{{ event.message }}</content>
        <author>{{ config.SITE.author }}</author>
        <sb:id>{{ event.key }}</sb:id>
        <sb:service>{{ event.service.name }}</sb:service>
        <sb:timestamp>{{ event.start|date:"Y-m-d\TH:i:s\Z" }}</sb:timestamp>
        <sb:message>{{ event.message }}</sb:message>
        <sb:url>{{ config.SITE.root_url }}/services/{{ event.service.key }}</sb:url>
        <sb:status>
            <sb:id>{{ event.status.key }}</sb:id>
            <title>{{ event.status.name }}</title>
            <sb:name>{{ event.status.name }}</sb:name>
            <sb:level>{{ event.status.get_level }}</sb:level>
        </sb:status>
    </entry>
    {% endfor %}
</feed>
